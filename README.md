# Selling Dubai

This is a minimal Next.js project demonstrating a property listings marketplace.

## Development

1. Install dependencies and start the dev server:

```bash
npm install
npm run dev
```

2. Copy `.env.example` to `.env.local` and provide your MongoDB connection string.
3. Tailwind CSS is preconfigured. Global styles are in `styles/globals.css`.

